# Add New Agent Workflow

Use the `/add-new-agent` slash-command workflow to add a new runtime agent.

Steps:
1. Invoke the `scaffold-runtime-agent` skill.
2. Follow its instructions step by step to scaffold the new folder, files, and update `state.py`.
3. Pause to ask the user if a guardrail is needed before continuing.
